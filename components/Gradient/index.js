import './gradient.scss';

function Gradient() {
  return (
    <div
      className="gradient"
      style={{
        background: 'linear-gradient(90deg,#ff0000,#00ff00)',
      }}
    />
  );
}

export default Gradient;
